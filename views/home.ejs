<%- include("partials/header"); -%>

<h1>Home</h1> 
<p> <%= homeText %> </p>

  <%  post1.forEach(function(post){ %>
    <!-- added toUpperCase() to transform header to upper case on home page-->
    <!-- added slice to truncate text on home page -->
        <h2> <%= post.title.toUpperCase() %> </h2>
        <p>  <%= post.inner.slice(0,200) + "..." %> <a href="post/<%= post.title %>">Read more</a> </p>
   <% }) %>
<%- include("partials/footer"); -%>